# Python 3 Integration Module — Architecture

> **This document has moved.** The authoritative, verified architecture guide is now
> **[ARCHITECTURE.md](ARCHITECTURE.md)** (with an interactive component map in
> **[architecture.html](architecture.html)**).
>
> The previous `OVERVIEW.md` content described a v2/v3 designer layout (`Python3IDE_v2`,
> `MetadataPanel`) and old `com.inductiveautomation.*` package paths that no longer match the
> source. It was replaced in v4.1.0 — see `ARCHITECTURE.md` §13 for the audit behind the rewrite.
